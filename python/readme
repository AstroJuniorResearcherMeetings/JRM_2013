These are tools for learning modern programming in astronomy. Python provides a language with takes advantage of objects and classes as well as being dynamically typed. This makes it easy to create code to quickly analyze data and which is resusable.